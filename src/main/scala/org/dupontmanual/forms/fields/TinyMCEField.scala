package org.dupontmanual.forms.fields

import scala.xml.{Attribute, MetaData, Null, Text}

import org.dupontmanual.forms.validators._
import org.dupontmanual.forms.widgets._

/*
 * TODO: yet to be impletemented.
 */


/*
  class TinyMCEField(name: String, tinyMCE: Boolean = true) extends TextField(name) {
  override def widget = new Textarea(required, Null, tinyMCE)
}

class OptionalTinyMCEField(name: String, tinyMCE: Boolean = true) extends TextFieldOptional(name) {
  override def widget = new Textarea(required, Null, tinyMCE)
}

*/
